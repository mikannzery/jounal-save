export default function Loading() {
  return (
    <div className="border-2 border-black bg-white p-8">
      <p className="text-xs font-semibold uppercase tracking-[0.24em]">Loading</p>
      <p className="mt-3 text-2xl font-black uppercase">ページを読み込んでいます。</p>
    </div>
  );
}
